<div class='jumbotron'>
  <p class='h1 text-center'><%= jumbo_text %></p>
  <% if local_assigns[:sub_text] %>
    <p><%= sub_text %></p>
  <% end %>
  <% if local_assigns[:button_text] %>
    <p class='text-center'><%= link_to button_text, url, class: 'btn btn-primary btn-lg' %></p>
  <% end %>
</div>
